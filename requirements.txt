# =============================================================
# Real-Time AI Translator - Project Dependencies
# =============================================================
# Install with:  pip install -r requirements.txt
# (with the virtu   al environment activated!)
#
# ⚠️  ATTENTION (NVIDIA GPU):
# If you have an NVIDIA graphics card and want to use CUDA, DO NOT install
# torch here. Go to https://pytorch.org/get-started/locally/
# and get the specific command for your card before installing
# the rest of the dependencies.
# =============================================================

# AI and Processing Engines
torch>=2.0.0
transformers>=4.30.0
sentencepiece>=0.1.99
numpy>=1.24.0

# Audio (capture and playback)
sounddevice>=0.4.6
scipy>=1.10.0

# Graphical Interface
customtkinter>=5.2.0
